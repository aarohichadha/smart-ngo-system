{
  "name": "smart-ngo-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend && npm --prefix ml-backend run install:py",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "test:frontend": "npm --prefix frontend run test",
    "dev:backend": "npm --prefix backend run dev",
    "start:backend": "npm --prefix backend run start",
    "dev:ml": "npm --prefix ml-backend run dev",
    "start:ml": "npm --prefix ml-backend run start"
  },
  "description": "Monorepo root for Smart NGO System (frontend, backend, ml-backend).",
  "dependencies": {
    "firebase": "^12.12.0"
  }
}
